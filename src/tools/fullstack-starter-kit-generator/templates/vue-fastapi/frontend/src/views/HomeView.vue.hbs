<script setup lang='ts'>
  import { useAuthStore } from '@/stores/auth' const authStore = useAuthStore()
</script>

<template>
  <div class='bg-white shadow rounded-lg p-6'>
    <h2 class='text-2xl font-bold mb-4'>Welcome to {{projectName}}</h2>
    <p class='text-gray-600'>{{description}}</p>
    <div v-if='authStore.user' class='mt-4'>
      <p class='text-gray-700'>You are logged in as
        {{authStore.user.full_name}}</p>
    </div>
  </div>
</template>