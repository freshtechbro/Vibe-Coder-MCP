from typing import List from pydantic import AnyHttpUrl, PostgresDsn from
pydantic_settings import BaseSettings, SettingsConfigDict class
Settings(BaseSettings): PROJECT_NAME: str = "{{projectName}}"
PROJECT_DESCRIPTION: str = "{{description}}" API_V1_STR: str = "/api" # CORS
CORS_ORIGINS: List[AnyHttpUrl] = ["http://localhost:3000"] # Database
POSTGRES_SERVER: str = "localhost" POSTGRES_USER: str = "postgres"
POSTGRES_PASSWORD: str = "postgres" POSTGRES_DB: str = "{{kebabCase
  projectName
}}" DATABASE_URI: PostgresDsn | None = None @property def
SQLALCHEMY_DATABASE_URI(self) -> str: if self.DATABASE_URI: return
str(self.DATABASE_URI) return
f"postgresql://{self.POSTGRES_USER}:{self.POSTGRES_PASSWORD}@{self.POSTGRES_SERVER}/{self.POSTGRES_DB}"
model_config = SettingsConfigDict( env_file=".env", case_sensitive=True, )
settings = Settings()