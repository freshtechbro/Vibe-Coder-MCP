{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.6.0",

  "description": "{{description}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "turbo": "^1.12.0",
    "typescript": "^5.0.0"
  }
}
