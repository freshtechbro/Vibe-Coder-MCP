# 🔄 VibeCoder Update Guide - KEEP YOUR SETTINGS

## 📋 BEFORE UPDATING VIBECODER

### Files that survive updates (DO NOTHING):
✅ `C:\Users\Ascension\Claude\root\my-llm-config.json`
✅ `C:\Users\Ascension\Claude\root\claude_desktop_config.json`
✅ `C:\Users\Ascension\Claude\root\vibe-coder-mcp\.env`
✅ All your project files in `/filesystem/`

### Files that get overwritten:
❌ Everything in `vibe-coder-mcp/src/` (includes .env switching system)

## 🚀 UPDATE PROCESS

### Step 1: Update VibeCoder
```bash
cd C:\Users\Ascension\Claude\root\vibe-coder-mcp
git pull origin main
# OR download new version and overwrite
npm run build
```

### Step 2: Restore Your Settings (Choose One Option)

#### Option A: Quick Restore Script (RECOMMENDED)
```
Double-click: C:\Users\Ascension\Claude\root\restore-vibecoder-after-update.bat
```
**What it does:**
- Restores .env model switching system
- Rebuilds VibeCoder automatically  
- Takes 30 seconds

#### Option B: Manual Restore (if script fails)
1. Copy the configLoader.ts content from `COMPLETE-VIBECODER-SETUP.md`
2. Paste into `vibe-coder-mcp\src\utils\configLoader.ts`
3. Run: `npm run build`

#### Option C: Use External Config Only (No Restore Needed)
- Your `my-llm-config.json` already has free models
- Edit that file to change models (no .env switching)
- 100% update-safe

### Step 3: Restart Claude Desktop
- Close Claude Desktop completely
- Reopen Claude Desktop
- Test: `curate-context for my debug table issue`

## ⚡ QUICK UPDATE WORKFLOW

1. **Update VibeCoder** (git pull or download)
2. **Run:** `restore-vibecoder-after-update.bat`
3. **Restart Claude Desktop**
4. **Done!** (Takes 2 minutes total)

## 🔧 CHANGING MODELS AFTER UPDATE

### With .env Switching (after restore):
Edit: `C:\Users\Ascension\Claude\root\vibe-coder-mcp\.env`
```
KIMI_MODEL="moonshotai/kimi-dev-72b:free"
LLAMA_MODEL="meta-llama/llama-3.3-70b-instruct:free"
```
Restart Claude Desktop

### With External Config (always works):
Edit: `C:\Users\Ascension\Claude\root\my-llm-config.json`
```json
{
  "llm_mapping": {
    "prd_generation": "meta-llama/llama-3.3-70b-instruct:free",
    "rules_generation": "moonshotai/kimi-dev-72b:free"
  }
}
```
Restart Claude Desktop

## 🎯 MODEL RECOMMENDATIONS

### Current Best FREE Models:
- **Code:** `moonshotai/kimi-dev-72b:free` (software engineering optimized)
- **General:** `meta-llama/llama-3.3-70b-instruct:free` (70B parameters)
- **Reasoning:** `deepseek/deepseek-r1-0528-qwen3-8b:free` (analysis)
- **Research:** `perplexity/sonar-deep-research` (web research, small cost)

### If Free Models Are Slow/Limited:
- **Upgrade to:** `google/gemini-2.5-flash-preview-05-20` (~$0.001/1K tokens)
- **Cost:** ~$2-8/month for 5+ projects

## 🚨 TROUBLESHOOTING

### If restore script fails:
1. Check if WSL is installed (needed for building)
2. Run script as Administrator
3. Use Option B (manual restore) instead

### If models aren't switching:
1. Check .env file has correct syntax (no extra quotes/spaces)
2. Restart Claude Desktop completely
3. Check logs in `%APPDATA%\Claude\logs\`

### If VibeCoder won't start:
1. Check Claude Desktop config points to correct paths
2. Verify build completed successfully
3. Try reverting to external config only (Option C)

## ✅ SUCCESS INDICATORS

After update + restore, you should see:
- VibeCoder tools work: `curate-context`, `map-codebase`, etc.
- Models from .env are used (check debug logs)
- Free model costs: $0-5/month
- All your project files intact

**Total update time: 2-5 minutes per VibeCoder release**